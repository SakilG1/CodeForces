#include <bits/stdc++.h>
using namespace std;
#define lli long long int
#define fn for (i = 0; i < n; i++)
int main()
{
    lli i;
    lli k, l, m, n, d;
    cin >> k >> l >> m >> n >> d;

    lli c=0;

    for(i=1; i<=d; i++)
    {
        if(i%k==0 || i%l==0 || i%m==0 || i%n==0)
        {
            c++;
        }
    }

    cout << c << endl;

    return 0;
}
