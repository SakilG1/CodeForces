#include <bits/stdc++.h>
using namespace std;
#define lli long long int

int main()
{
    lli arr[3];
    cin >> arr[0] >> arr[1] >> arr[2];
    sort(arr, arr+3);
    cout << (arr[1]-arr[0])+(arr[2]-arr[1]) << endl;
    return 0;
}
