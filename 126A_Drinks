#include<bits/stdc++.h>
using namespace std;
int main()
{

    int i, n;
    cin >> n;
    double arr[n];

    double  sum=0;
    for(i=0; i<n; i++)
    {
        cin >> arr[i];
        sum += arr[i];
    }

    cout << fixed << setprecision(12) << sum/n << endl;


    return 0;
}
