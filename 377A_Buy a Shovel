#include <bits/stdc++.h>
using namespace std;
#define lli long long int

int main()
{
    lli k, r;

    cin >> k >> r;
    lli c=0, i;

    for(i=1; i; i++)
    {
        if((k*i)%10==r || (k*i)%10==0)
        {
            cout << i << endl;
            break;
        }
    }

    return 0;
}
