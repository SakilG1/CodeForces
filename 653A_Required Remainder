#include <bits/stdc++.h>
using namespace std;
#define lli long long int
int main()
{

    lli t = 1;
    cin >> t;
    while (t--)
    {
        lli x, y, n, i;
        cin >> x >> y >> n;

        i = x*((n-y)/x)+y;
        cout << i << endl;


    }
    return 0;
}
